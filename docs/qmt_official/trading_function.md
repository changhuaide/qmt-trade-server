# 交易函数

> 来源: https://dict.thinktrader.net/innerApi/

## 交易下单函数## passorder - 综合下单函数综合下单函数，用于股票、期货、期权等下单和新股、新债申购、融资融券等交易操作**推荐使用**

提示

- 推荐使用
- 可覆盖多品种下单
- 注意参数的变化

**调用方法：**

下单的账号ID（可多个）或账号组名或套利组名（一个篮子一个套利账号，如 accountID = '股票账户名, 期货账号'）

下单选价类型

下单数量（股 / 手 / 元 / %）

自定义策略名

设定是否立即触发下单

用户自设委托 ID

ContextInfo

class

含有k线信息和接口的上下文对象

用于按固定时间间隔和固定规则把目标交易数量拆分成多次下单的交易函数

调用该函数需要有【智能算法】使用权限

用于使用主动算法或被动算法交易的函数如VWAP TWAP等

可选参数可缺省

prType

smartAlgoType

limitOverRate

量比 数据范围0-100

minAmountPerOrder

智能算法最小委托金额，数据范围0-100000

targetPriceLevel

startTime

智能算法开始时间

endTime

智能算法截止时间

limitControl

涨跌停控制

accountID

string

strAccountType

string

strDatatype

string

strategyName

string

获取最新的委托或成交的委托号

注意，下单后需要一段不确定的时间才能查询到此次委托的委托号，而且如果委托废单，则只能查询到上次成功下单的委托号

accId：信用账户

本函数一次最多查询200只股票的两融最大下单量，且同时只能有一个查询,如果前面的查询正在进行中,后面的查询将会提前返回。本函数从服务器查询数据,建议平均查询时间间隔180s一次,不可频繁调用。

本函数一次最多查询200只股票的两融最大下单量，且同时只能有一个查询,如果前面的查询正在进行中,后面的查询将会提前返回。本函数从服务器查询数据,建议平均查询时间间隔180s一次,不可频繁调用。

accountID：string,账号